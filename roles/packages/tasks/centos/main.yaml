---
- name: 安装通用软件包
  dnf:
    name: "{{ common_packages }}"
    state: present
    update_cache: yes
  tags:
    - packages

- name: 安装 CentOS 特定软件包
  dnf:
    name: "{{ centos_packages }}"
    state: present
    update_cache: yes
  tags:
    - packages
