---
- name: 安装通用软件包
  apt:
    name: "{{ common_packages }}"
    state: present
    update_cache: yes
  tags:
    - packages

- name: 安装 Debian 特定软件包
  apt:
    name: "{{ debian_packages }}"
    state: present
    update_cache: yes
  tags:
    - packages
