---
- name: 安装通用软件包
  apt:
    name: "{{ common_packages }}"
    state: present
    update_cache: yes

- name: 安装 Ubuntu 特定软件包
  apt:
    name: "{{ ubuntu_packages }}"
    state: present
    update_cache: yes 