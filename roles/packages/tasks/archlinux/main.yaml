---
- name: 安装通用软件包
  yay:
    name: "{{ common_packages }}"
    state: present
    update_cache: yes

- name: 安装 Arch Linux 特定软件包
  yay:
    name: "{{ archlinux_packages }}"
    state: present
    update_cache: yes 